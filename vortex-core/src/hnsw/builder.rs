//! Contains logic related to building the HNSW index, primarily parameter handling.
// Currently empty as config validation is in config.rs and building logic is in index.rs
// This can be expanded later if index construction becomes more complex.
